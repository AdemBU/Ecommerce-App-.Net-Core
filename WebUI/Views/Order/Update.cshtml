@model Order

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Update</title>
</head>
<body>
    <div class="container mt-4">
        <h4>Sipariş Düzenle (#@Model.OrderId)</h4>
        <hr />
        <form asp-action="Update">
            <input type="hidden" asp-for="OrderId" />
            <div class="row">
                <div class="col-md-6 mb-3">
                    <label class="form-label">Müşteri Adı</label>
                    <input asp-for="UserName" class="form-control" />
                </div>
                <div class="col-md-6 mb-3">
                    <label class="form-label">Toplam Tutar</label>
                    <input asp-for="TotalPrice" type="number" step="0.01" class="form-control" />
                </div>
                <div class="col-md-12 mb-3">
                    <label class="form-label">Sipariş Detayları</label>
                    <textarea asp-for="OrderDetails" class="form-control" rows="3"></textarea>
                </div>
            </div>
            <button type="submit" class="btn btn-warning">Güncelle</button>
            <a asp-action="Index" class="btn btn-secondary">Geri Dön</a>
        </form>
    </div>
</body>
</html>
